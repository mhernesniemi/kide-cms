import type { SeedDocument } from "../core";

// Kide ships no seed content. Define your own documents here (keyed by
// collection slug) and run `kide seed` to load them into an empty database.
const seedData: Record<string, SeedDocument[]> = {};

export default seedData;
