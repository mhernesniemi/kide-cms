---
import { renderBlocks } from "../cms/core/blocks";

const { blocks = [] } = Astro.props as { blocks?: Array<Record<string, unknown>> };
const html = renderBlocks(blocks);
---

<Fragment set:html={html} />
