{
  "dependencies": {
    "add": { "@astrojs/cloudflare": "~14.1.7" },
    "remove": ["@astrojs/node", "sharp"]
  },
  "devDependencies": {
    "add": { "wrangler": "^4.121.0" },
    "moveFromDependencies": ["better-sqlite3"]
  },
  "scripts": {
    "preview": "astro build && wrangler dev --config dist/server/wrangler.json",
    "deploy": "astro build && wrangler deploy --config dist/server/wrangler.json"
  }
}
